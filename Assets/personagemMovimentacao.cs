using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class personagemMovimentacao : MonoBehaviour {
	private GameObject seta;
	public float xPosition,yPosition,zPosition;
	public GameObject player;
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}

	public void OnMouseDown ()
    {
        /*if (seta.rotation == seta.rotation(0,0,90))
		{
			player.transform.position(xPosition,yPosition+1,zPosition);
		}
		*/
		//player.transform.position();
    }
}
